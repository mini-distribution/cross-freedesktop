# Description: X.Org xinerama library
# URL: http://xorg.freedesktop.org

name=libXinerama
version=1.1.2
release=1
source=(http://xorg.freedesktop.org/releases/individual/lib/$name-$version.tar.bz2)
depends=(libX11 xineramaproto libXext)
#depends=(libX11 randrproto xextproto libXext libXrender renderproto)

build() {
   cd $name-$version

   ./configure --build=$BUILD \
               --host=$HOST \
               --prefix=/usr \
               --enable-malloc0returnsnull

   make -j $JOBS
   make DESTDIR=$PKG install

   # Fix libtool files
   fix_la_files $PKG
}
